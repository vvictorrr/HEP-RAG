# Core
python-dotenv==1.0.1
requests==2.32.3
tqdm==4.66.5

# PDF parsing
PyMuPDF==1.24.10

# Retrieval
sentence-transformers==3.1.1
chromadb==0.5.5
rank-bm25==0.2.2
adapters==1.0.1   # required to load allenai/specter2's proximity adapter onto specter2_base
                  # if you hit a version conflict with transformers==4.44.2 below, run
                  # `pip install -U adapters` and let pip resolve a compatible pair

# Reranking / NLI (cross-encoders load via sentence-transformers or transformers)
transformers==4.44.2
torch==2.4.1

# Generation / judging
anthropic==0.34.2
httpx==0.27.2   # anthropic==0.34.2 passes a `proxies=` kwarg internally that httpx>=0.28 removed;
                # pin here rather than bumping anthropic, since chromadb/streamlit may also pull in httpx

# Frontend
streamlit==1.38.0
plotly==5.24.0
pandas==2.2.2
matplotlib==3.9.2   # required by pandas Styler.background_gradient() in src/app.py's Benchmark tab

# Eval / misc
numpy==1.26.4
scikit-learn==1.5.1